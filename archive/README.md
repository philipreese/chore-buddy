# Archive

Frozen reference material from the pre-Flutter era of this project: `ChoreBuddy-maui/` (the .NET MAUI implementation, plus its `ChoreBuddy.sln` and the C#-specific `.agent/` coding rules), superseded by the Flutter app in `app/` per [ADR-0001](../docs/adr/0001-migrate-to-flutter-android-only.md). It exists purely as a behavioral reference for the migration (see `docs/behavior-inventory.md`) and is not built, run, or maintained; delete it once confidence in `app/` parity has settled.
